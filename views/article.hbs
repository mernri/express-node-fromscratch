<body>
    <div class='container'>
        <h1>Article details</h1>
        <h3> {{article.title}}</h3>
        <h5> Written by {{article.author}}</h5>
        <p> {{article.body}}</p>
        <a type="button" class="btn primary" href="/articles/{{article._id}}/edit"> Edit Article </a>
        <form action="/articles/{{article._id}}/delete" method="post">
            <button type="submit">Delete article</button>
        </form>
    </div>
</body>